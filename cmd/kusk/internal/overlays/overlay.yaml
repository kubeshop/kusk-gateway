overlays: 1.0.0
extends: ./test.yaml
actions:
  - target: "$"
    update:
      x-kusk:
        upstream:
          service:
            name: httpbin
            namespace: default
            port: 8080
