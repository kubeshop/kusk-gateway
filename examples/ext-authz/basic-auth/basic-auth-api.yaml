apiVersion: gateway.kusk.io/v1alpha1
kind: API
metadata:
  name: ext-authz-http-basic-auth
spec:
  fleet:
    name: default
    namespace: default
  # service name and port should be specified inside x-kusk annotation
  spec: |
    openapi: 3.0.0
    info:
      title: ext-authz-http-basic-auth
      description: ext-authz-http-basic-auth
      version: '0.0.1'
    schemes:
    - http
    - https
    x-kusk:
      auth:
        scheme: basic
        auth-upstream:
          host:
            hostname: ext-authz-http-basic-auth
            port: 80
      upstream:
        service:
          name: ext-auth-http-oauth2-httpbin-basic-auth
          namespace: default
          port: 80
    paths:
      "/":
        get:
          description: Returns GET data.
          operationId: "/get"
          responses: {}
      "/uuid":
        get:
          description: Returns UUID4.
          operationId: "/uuid"
          responses: {}
